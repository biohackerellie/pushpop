{
  "name": "pushpop/root",
  "version": "0.0.0",
  "scripts": {
    "release": "changeset version",
    "publish": "cd ts-client && npx np",
    "check:ts": "cd ts-client && pnpm run lint",
    "build": "cd ts-client && pnpm run build"
  },
  "dependencies": {
    "@changesets/changelog-github": "^0.4.8",
    "@changesets/cli": "^2.27.3"
  },
  "packageManager": "pnpm@9.14.4"
}
